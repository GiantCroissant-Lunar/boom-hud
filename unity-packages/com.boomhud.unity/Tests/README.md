# BoomHud Unity Package Tests

This folder is reserved for Unity Test Runner coverage for the `com.boomhud.unity` package.

Suggested future split:

- EditMode tests for package editor utilities and host lifecycle behavior.
- PlayMode tests for UI Toolkit and uGUI host integration.